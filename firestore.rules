rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signed() { return request.auth != null; }
    match /nexusTransport/{docId} {
      allow read, write: if signed();
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
